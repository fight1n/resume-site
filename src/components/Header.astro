---
import { profile } from '../data/profile';
const base = import.meta.env.BASE_URL;

const path = Astro.url.pathname;
const navItems = [
  { href: base, label: '首页' },
  { href: base + 'articles/', label: '文章' },
  { href: base + 'projects/', label: '项目' },
  { href: base + 'about/', label: '关于' },
];

function isActive(href: string): boolean {
  if (href === base) return path === base || path === base.replace(/\/$/, '');
  return path.startsWith(href);
}
---

<header class="site-header">
  <div class="container">
    <a class="brand" href={base}>{profile.name}<span>.</span></a>
    <button class="nav-toggle" aria-label="打开菜单" aria-expanded="false">
      <span></span>
    </button>
    <nav class="nav" id="primary-nav">
      {
        navItems.map((item) => (
          <a href={item.href} class={isActive(item.href) ? 'active' : ''}>
            {item.label}
          </a>
        ))
      }
    </nav>
  </div>
</header>

<script>
  const btn = document.querySelector<HTMLButtonElement>('.nav-toggle');
  const nav = document.getElementById('primary-nav');
  btn?.addEventListener('click', () => {
    const open = nav?.classList.toggle('open');
    btn.setAttribute('aria-expanded', String(Boolean(open)));
  });
</script>
